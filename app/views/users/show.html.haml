%h1 Profile
=render 'form'
%hr

%br
%h1 Hosting Events 
- @events.each do |event|
    %p=link_to "#{event.title} - #{event.time.strftime("%B %d, %Y")}", event_path(event) 
%hr
%h1 Attending Events 
- @attending_events.each do |event|
    %p=link_to "#{event.title} - #{event.time.strftime("%B %d, %Y")}", event_path(event) 